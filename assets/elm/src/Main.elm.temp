deleteTodo : String -> Cmd Msg
deleteTodo id =
    Http.request
        { method = "DELETE"
        , headers = []
        , url = "/api/todos/" ++ id
        , body = Http.emptyBody
        , expect = Http.expectWhatever (TodoDeleted id)
        , timeout = Nothing
        , tracker = Nothing
        }
